apiVersion: v1
kind: ServiceAccount
metadata:
  name: jenkins
  namespace: jenkins
  labels:
    app: jenkins
    component: controller
  annotations:
    description: "Service account for Jenkins controller and agents"
# Token will be auto-mounted to pods that use this SA
automountServiceAccountToken: true
